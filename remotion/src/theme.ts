/**
 * 色とフォントの単一の情報源。
 *
 * フォントは **Web フォントを使わない**。`fonts-noto-cjk`（本番イメージに
 * 既に入っている）を font-family で参照する。@font-face で非同期に読ませると、
 * delayRender / waitForFonts で待たない限り最初の数フレームだけ
 * フォールバックフォントで焼かれ、エラーにならないので気付きにくい。
 *
 * 代償: ローカル（Windows / Yu Gothic）と本番（Linux / Noto Sans CJK）で
 * 字形が変わる。最終確認は Docker 経由で行う。
 */
export const FONT_STACK =
  '"Noto Sans CJK JP", "Noto Sans JP", "Yu Gothic", "Meiryo", "Hiragino Sans", sans-serif';

/**
 * 「白墨 on スレート」から「フラット」へ。
 *
 * 地の暗さは残す（明るい紙ではなく暗い地でいく、というオーナーの選択は
 * 変えない）が、彩度を落とした「手描きチョーク」の配色は実物のレンダリング
 * では**線が細く、絵柄も相まって「何を言っているか一目で分からない」**
 * という判断をオーナーから受けた。手描きの揺れをやめてフラットな図に
 * 振る以上、色も彩度を落とした中間色ではなく、はっきり見分けられる
 * ティール／アンバーに上げる（スケッチの質感が無い分、色のコントラストで
 * 「意図して選んだ配色」だと伝える必要がある）。
 */
export const COLORS = {
  // スレート (#242226) よりニュートラルで暗い黒炭色。
  // 暖色寄りのスレートは「布」や「石板」の質感を連想させ、フラットな
  // 図形と合わせると中途半端に見えたため、より中立な黒に寄せた。
  bg: "#1b1a1d",
  // 純白ではなく、わずかに暖色を残したオフホワイト。
  text: "#f5f2ea",
  // 字幕は白文字。黄色＋黒ボックスは使わない。
  subtle: "rgba(245,242,234,0.85)",
  // チョーク・ティール (#5ea79c) から彩度・明度を上げ、はっきり見分けられる
  // フラットなティールに。
  accent: "#2dd4bf",
  // チョーク・アンバー (#c98a4c) から彩度を上げ、フラットなアンバーに。
  accent2: "#f2a93c",
  // 文字の背後に敷く、地の色より少し明るいプレート。
  // 以前は手描き線の斜線塗りと文字がぶつかるのを避けるためだけの用途
  // だったが、フラット化後も「地に文字を直接置くと縁が甘くなる」場面
  // （`ChapterTag` など）でそのまま使う。
  plate: "#272529",
} as const;
