/**
 * 色とフォントの単一の情報源。
 *
 * フォントは **Web フォントを使わない**。`fonts-noto-cjk`（本番イメージに
 * 既に入っている）を font-family で参照する。@font-face で非同期に読ませると、
 * delayRender / waitForFonts で待たない限り最初の数フレームだけ
 * フォールバックフォントで焼かれ、エラーにならないので気付きにくい。
 *
 * 代償: ローカル（Windows / Yu Gothic）と本番（Linux / Noto Sans CJK）で
 * 字形が変わる。最終確認は Docker 経由で行う。
 */
export const FONT_STACK =
  '"Noto Sans CJK JP", "Noto Sans JP", "Yu Gothic", "Meiryo", "Hiragino Sans", sans-serif';

/**
 * 「ネオン on 紺」から「白墨 on スレート」へ。
 *
 * 地の暗さは残す（明るい紙ではなく暗い地でいく、というオーナーの選択は
 * 変えない）が、純白＋高彩度アクセントは工業製品的な UI に見える
 * （このプロジェクトが離れようとしている「AI っぽさ」そのもの）。
 * 黒板に白墨で書いたような、彩度を落とした配色にする。
 */
export const COLORS = {
  // 紺黒 (#0b1020) より暖色寄りのスレート／墨色。
  bg: "#242226",
  // 純白ではなく、白墨のようなクリーム色。
  text: "#f3ecdf",
  // 字幕は白文字。黄色＋黒ボックスをやめるのがこの作業の目的の1つ。
  subtle: "rgba(243,236,223,0.82)",
  // #4cc9f0 を彩度を落とした青緑（チョーク・ティール）に。
  accent: "#5ea79c",
  // #f72585 を彩度を落とした琥珀（チョーク・アンバー）に。
  accent2: "#c98a4c",
  // ハッチング塗りの上にラベルを置くための、地の色に近いプレート。
  // 手描き線の上に文字を直接置くと斜線と字がぶつかって読めなくなるため、
  // 文字の背後だけ塗りを避ける（Rough.tsx / 各シーンから使う）。
  plate: "#2d2a2f",
} as const;
