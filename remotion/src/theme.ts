/**
 * 色とフォントの単一の情報源。
 *
 * フォントは **Web フォントを使わない**。`fonts-noto-cjk`（本番イメージに
 * 既に入っている）を font-family で参照する。@font-face で非同期に読ませると、
 * delayRender / waitForFonts で待たない限り最初の数フレームだけ
 * フォールバックフォントで焼かれ、エラーにならないので気付きにくい。
 *
 * 代償: ローカル（Windows / Yu Gothic）と本番（Linux / Noto Sans CJK）で
 * 字形が変わる。最終確認は Docker 経由で行う。
 */
export const FONT_STACK =
  '"Noto Sans CJK JP", "Noto Sans JP", "Yu Gothic", "Meiryo", "Hiragino Sans", sans-serif';

/**
 * 「白墨 on スレート」→「フラット」→「寒色の先端技術」へ。
 *
 * 扱う題材は AI・クラウド・先端技術の解説である。それに対して以前の配色は
 * **クリーム色の紙・深いティール・焦げた琥珀**という、教科書や印刷物の
 * 語彙だった。暖色はアナログで安全な印象を作り、題材と合っていない
 * （2026-08-20、オーナーの判断）。
 *
 * 先端技術の視覚言語は**寒色・高コントラスト・電子的なアクセント**である。
 * そこで全体を寒色に振る。地は暖色寄りの黒炭からグラファイト（青みのある
 * ほぼ黒）へ、アクセントは琥珀を捨てて電気的なバイオレットへ。
 *
 * **グラデーションと発光は使わない。** 「先進的」の記号として最も手軽なのが
 * グラデーションと glow だが、それは**汎用的な AI 生成画像の署名そのもの**で
 * ある。このプロジェクトは「AIくささが前面に出ていて見たいと思いづらい」
 * という出発点から始まっているので、そこへ戻る配色は選べない。
 * フラットなまま、寒色と電気的なアクセントで先進性を作る。
 */
export const COLORS = {
  // 青みのあるグラファイト。以前は #1b1a1d（暖色寄りの黒炭）で、
  // 挿絵のインクと揃えてある（同じ「ほぼ黒」を紙の上と地の両方で使う）。
  bg: "#14161a",
  // 純白ではなく、わずかに寒色へ寄せたオフホワイト。
  // 以前は #f5f2ea（暖色のクリーム）だった。
  text: "#eef1f5",
  // 字幕は白文字。黄色＋黒ボックスは使わない。
  subtle: "rgba(238,241,245,0.85)",
  // 電気的なシアン。以前の #2dd4bf（ミントに寄ったティール）より青く、
  // 寒色の地の上でより「電子的」に見える。
  accent: "#22d3ee",
  // 第2アクセント。**琥珀（#f2a93c）から置き換えた。** 暖色の琥珀は
  // 寒色に統一した配色の中で1つだけ浮き、印刷物の印象を持ち込む。
  accent2: "#8b7cff",
  // 文字の背後に敷く、地の色より少し明るいプレート。
  // 以前は #272529（暖色寄り）。地と同じ寒色方向に合わせる。
  plate: "#1d2027",
  // 挿絵カードの地（紙）。**`text` と同じ値だが別のトークンにしてある。**
  // 役割が違うので、一方を調整したときにもう一方が黙って動くのを避ける
  // （文字の色を少し明るくしたら紙の色も変わる、という事故を防ぐ）。
  // この値は `ILLUSTRATION_STYLE_PROMPT` が画像モデルに指示する紙の色と
  // 一致させること——ずれると挿絵の縁がカードの中で見える。
  paper: "#eef1f5",
} as const;
